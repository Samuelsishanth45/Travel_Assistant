#include "json_parser.h"
#include <vector>
using namespace std;

// json_parser.cpp is NO LONGER USED.
// We keep only an empty stub to avoid duplicate definitions.

vector<Train> loadTrainsFromJSON(const string& filename) {
    return {};  // Do nothing (CSV parser handles JSON now)
}





